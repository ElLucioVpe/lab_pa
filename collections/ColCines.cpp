#include "ColCines.cpp"

void ColCines::add(Cine* c) {
    add(c);
}

void ColCines::remove(Cine* c) {
    remove(c);
}

bool ColCines::member(Cine* c) const{
    member(c);
}

int ColCines::getSize() const {
    getSize(c);
}

bool ColCines::isEmpty() const {
    isEmpty(c);
}